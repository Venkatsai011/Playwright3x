//let 1name; //error because identifier cannot start with a number
let name1; //valid identifier because the number is not at the start

//the identifiers should not contain any special characters except for $ and _

let _name; //valid identifier
let $name; //valid identifier

//let name@; //error because identifier cannot contain special characters

//let for =1; //error because for is a reserved keyword in JavaScript

//let while =1; //error because while is a reserved keyword in JavaScript

//let if =1; //error because if is a reserved keyword in JavaScript

